f"""You are reviewing a detected change region between two revisions
    of an engineering drawing. OCR extracted this text from each side:

    OLD: "{old_text}"
    NEW: "{new_text}"
    Rule-based category guess: "{rule_based_category}"

    Look at the two attached cropped images (old, then new) to confirm what
    changed. Do not invent any number or word not visible in the images or
    OCR text above.

    Respond with ONLY valid JSON, no markdown fences:
    {{"category": one of {sorted(VALID_CATEGORIES)}, "description": a short
    one-sentence description, "confidence": a number between 0 and 1}}"""